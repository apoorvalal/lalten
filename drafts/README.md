# Drafts

In-progress, self-contained HTML drafts live here.

Public URL pattern:

- `https://lalten.org/drafts/<filename>.html`

Use this for temporary/shareable draft artifacts that should have a cleaner URL than `/pages/`.
